// actions.ts
import { EServiceNames } from '@constants';

// eventually we can split each service/feature actions into their own files.
export const EAction = {
  [EServiceNames.SERV_RM]: {
    LIKE: 'add char to favorites',
  },
};
